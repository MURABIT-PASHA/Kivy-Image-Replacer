MDScreen:
    name: 'painter'
    MDBoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 20

        FileChooserIconView:
            id: filechooser
            on_selection: app.selection(filechooser.selection)